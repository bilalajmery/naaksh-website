import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact Us | NAAKSH — Streetwear Pakistan',
  description: 'Get in touch with NAAKSH. Questions regarding orders, custom sizes, collaborations or support.',
};

export default function ContactPage() {
  return <ContactClient />;
}
